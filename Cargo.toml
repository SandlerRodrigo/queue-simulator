[package]
name = "queue_simulator"
version = "0.1.0"
edition = "2021"
description = "Simulador de Filas de Eventos Discretos (G/G/c/K) baseado na notação de Kendall"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
